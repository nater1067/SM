{% extends 'SMChatBundle:layout:layout.html.twig' %}
{% block body %}
    {% trans_default_domain 'FOSUserBundle' %}

    <form action="{{ path('model_registration') }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'registration.submit'|trans }}" />
        </div>
    </form>
{% endblock body %}