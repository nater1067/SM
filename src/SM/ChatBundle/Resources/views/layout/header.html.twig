<header class="navbar navbar-static-top bs-docs-nav" id="top" role="banner">
    <div class="container">
        <div class="navbar-header">
            <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".bs-navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="../" class="navbar-brand">Secret Mates</a>
        </div>
        <nav class="collapse navbar-collapse bs-navbar-collapse" role="navigation">
            <ul class="nav navbar-nav">
                <li class="active">
                    <a href="{{ path('model_registration') }}">Got talent? Sign up as a model here.</a>
                </li>
            </ul>
            {% if app.user %}
                <a href="{{ path('model_profile') }}">{{ app.user.firstName }}</a>
            {% else %}
                Not Logged In
            {% endif %}
            <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                    <li><a href="{{ path('logout') }}">Logout</a></li>
                {% else %}
                    <li><a href="{{ path('sm_chat.viewer_login') }}">Login</a></li>
                    <li><a href="{{ path('sm_chat.viewer_register') }}">Sign Up</a></li>
                {% endif %}
            </ul>
        </nav>
    </div>
</header>